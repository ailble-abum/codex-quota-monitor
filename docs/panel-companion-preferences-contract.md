# V2 伴宠动作与提醒偏好

2026-09-20，接续 92c64a9。移除固定外部源的动作、状态提醒、上下文提醒三个控件的初始化和监听。V2 用三个明确键的 Map、原生 Storage 与根 change 委托实现，不增加依赖。

三项保持原默认开启语义：持久值为字符串 false 才关闭，缺失/非法/读取失败回退开启。写入仅接受已知名称和布尔值；失败后按键保留当前挂载的临时选择，成功保存后清除临时值并重新允许读取外部变更。控件每次刷新都从同一偏好函数同步，语言重建也不丢失临时值。

生成器对五处旧行为读取做数量校验后替换：动作手势、动作响应、motion 数据属性，以及上下文/状态提醒事件过滤。行为与控件使用同一份值，关闭开关时清除当前提示并刷新。其余反馈计算、定时器、动画和提醒文案仍是保留实现；不声称本轮替换整个伴宠。

## 验证

- verify_companion_preferences.cjs 在实现前缺文件失败，实现后通过三键默认值、非法值、无初始化写入、读取拒绝、写入失败、恢复、实例隔离及输入类型校验；既有语言和单位偏好测试通过。
- verify_mount 在旧候选复现存储写入失败后动作开关关闭但 data-motion 仍 true；新候选 Chromium/WebKit 通过 data-motion=false、三开关跨语言保留关闭、恢复后落盘 true 及动作状态同步，无页面错误。
- 完整候选通过 verify_panel --bridge 的双浏览器明暗主题、任务切换与失效数据，以及 verify_dispose 的卸载重挂载和所有权保护。Python 3.9.6 / 3.14.3 各 120 项通过。

候选 /tmp/quota-companion-options-candidate，截图 /tmp/quota-companion-options-components 与 /tmp/quota-companion-options-panel-evidence。行为验证包含浏览器属性与交互，不等于真实原生通知、Windows 或发行验收；真实健康/账户数据通路仍未接通。现用安装与真实偏好未改动。其他设置仍有直接 Storage 访问，不能据此声称全面支持禁用存储。保留资源与代码继续携带 LICENSE/NOTICE。下一步核查未接通的配额通知开关与其他布局设置。
