# 正式线功能对照与 V2 复用

2026-09-24 只读检查 `main` 工作区与其未提交改动；没有修改或暂存正式线文件。正式线既有的 `usage_history.py`、`QuotaMenu.swift`、`injector_status.py` 和 `quota_alerts.py` 属于本项目已有扩展，可复用其功能需求与数据含义，但其运行入口仍耦合旧注入器和目录布局，不能整文件搬入 V2。正式线 `payload_builder.py` 在没有活动任务摘要时会回退到第一条会话；V2 的身份关联规则禁止该回退。

| 正式线能力 | V2 状态 | 处理 |
| --- | --- | --- |
| 手动检查更新 | 此前只有定时查询 | 已按 V2 页面桥适配按钮和强制刷新，保持显式 HTTPS URL 校验 |
| 菜单栏与七天周报 | V2 已有独立菜单程序和面板采样报告；正式线有完整离线 HTML 周报 | 已迁入 V2 自有采样格式的离线 HTML 页面和菜单打开入口；正式线图表与官方活动统计仍未迁入 |
| 注入状态诊断 | V2 此前只有服务 `doctor` 和页面状态 | 已加入私有状态文件与 `doctor` 的面板发布检查；真实宿主恢复仍待验收 |
| 四角缩放、提醒位置 | V2 已有等效布局处理 | 保留 V2 实现，不重复搬运 |
| 配额通知 | V2 已有按账户/周期去重的可选通知 | 保留 V2 实现，不重复搬运 |

正式线当前存在暂存和未暂存的修改；迁移仅在 `codex/v2-beta` 实施，避免覆盖这些工作。复用核查基于本地已提交项目代码和合成测试；完整原生功能仍需单独验收。
