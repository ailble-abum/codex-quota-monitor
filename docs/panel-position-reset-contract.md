# V2 位置重置

2026-09-20，接续 bbcb85c。移除固定外部源的正文按钮监听与重置逻辑，改为面板根 click 委托调用 V2 resetPanelPosition。复用现有默认位置与视口约束，不增加依赖。

重置分别尝试移除旧位置键与布局键；任意删除失败不阻止另一项，也不阻止当前界面重置。取消停靠隐藏计时器，清空内存布局与停靠/边缘/固定/展开标记，再应用默认浮动位置。非空对象真值的空布局避免刷新时重新读取未删除的旧记录。函数返回值仅表示两项持久删除是否成功，未向用户宣称失败记录已清除。

保持旧操作清空两种模式几何的语义，包含已有自定义宽度；不更改尺寸预设偏好。删除不具备事务性，失败记录可在卸载/重启后重新生效；操作不承诺取消活动拖拽或修复所有布局状态。其他直接 Storage 访问仍有各自边界。

## 验证

- verify_docking 新增真实按钮回归在旧候选失败：删除抛错后 data-docked 仍 true。新候选 Chromium/WebKit 左右停靠均验证失败删除后的浮动可见、标记清理、两项删除都尝试、内存布局清空、伴宠隐藏、刷新不恢复旧停靠，以及恢复删除后两键均空且无页面异常。
- verify_position_reset.cjs 检查任一删除失败、同时失败及成功的返回值与独立尝试，并确认调用计时器清理和默认位置应用。
- 重置截图先等待过渡动画结束，并断言默认左侧坐标为 14px，避免使用动画中间帧。
- 完整挂载与既有设置回归通过。Python 3.9.6 / 3.14.3 各 120 项通过。

候选 /tmp/quota-position-reset-candidate，截图 /tmp/quota-position-reset-components 与 /tmp/quota-position-reset-docking。使用隔离合成会话与存储，现用安装及真实偏好未动；不构成原生、Windows 或发行验收。默认位置、拖拽、停靠及视觉资源仍保留来源归因与 LICENSE/NOTICE。下一步继续处理伴宠尺寸。
