# V2 边缘吸附偏好

2026-09-20，接续 ccd4fe0。移除旧 edgeDockEnabled 和正文中的开关初始化/change 监听，使用 V2 偏好读取、控件同步和根节点委托。不新增依赖，停靠检测与几何计算仍复用保留实现。

沿用默认开启语义，字符串 false 才关闭；缺失、非法或读取失败回退开启。写入仅接受布尔值，失败后在当前挂载保留临时选择，成功清除临时值。关闭时取消隐藏计时器、解除当前停靠并刷新；重新开启仅允许后续吸附，不立即改变当前浮动位置。临时选择不跨卸载/重启，布局与偏好仍是独立存储记录。

## 验证

- verify_edge_preference.cjs 实现前缺文件失败，之后通过默认值、读取拒绝、写入失败、非法输入及恢复。
- verify_edge_dock.cjs 在旧候选复现写入失败后仍停靠；新候选 Chromium/WebKit 左右边缘验证解除停靠、固定标记清理、伴宠隐藏、语言切换保持关闭、原存储未改、恢复保存，以及开启不强制重新停靠。无页面错误。
- 既有 verify_mount 和 verify_docking 回归通过；Python 3.9.6 / 3.14.3 各 120 项通过。已目视检查关闭吸附后的 WebKit 明暗截图。

候选 /tmp/quota-edge-candidate，截图 /tmp/quota-edge-evidence。证据限于隔离合成浏览器，不是原生、Windows 或发行验收；现用安装与真实偏好未动。保留布局、模板和艺术资源继续归因并携带 LICENSE/NOTICE。下一步处理皮肤选择。
