# V2 伴宠尺寸偏好

2026-09-20，接续 cc84839。替换旧 mascotScale、尺寸控件投影及正文 input/自动按钮监听，接入 V2 偏好函数与根节点委托；保留 companionScale 的自动尺寸计算和停靠几何，无新依赖。

手动值接受有限数值 0.75–2，持久数据先转数值再检查边界；缺失、非法或读取失败按自动。输入使用 range.valueAsNumber 的百分比换算。null 表示自动，undefined 表示没有临时覆盖；保存或删除失败都保留本次挂载选择，成功后清除临时值。实际 mascotScale、滑块、百分比标签和自动按钮 aria-pressed 共用偏好，刷新不再用旧存储值覆盖失败后的选择。

自动算法保持按逻辑视口宽/高计算，范围 100%–150%；手动范围 75%–200%。本轮未改变艺术资源、CSS 缩放方式和停靠布局。临时值不跨卸载/重启，也不自动重试失败写入；其他设置的存储访问仍有独立边界。

## 验证

- verify_scale_preference.cjs 实现前缺文件失败；实现后通过缺失/非法/边界值、读取失败、手动保存失败、自动删除失败、非法输入拒绝、恢复及外部偏好变化。
- verify_docking 在旧候选复现写入失败后键盘 End 将滑块改为 200% 但伴宠 CSS 比例仍为 1.25；新候选双浏览器左右停靠通过比例 2 和百分比同步、实际包围盒增宽、删除失败后自动比例 1、刷新保持自动、恢复写入 0.75 和成功删除。
- 完整挂载和既有停靠/位置重置回归通过。Python 3.9.6 / 3.14.3 各 120 项通过。

候选 /tmp/quota-scale-candidate，截图 /tmp/quota-scale-components 与 /tmp/quota-scale-docking。仅为隔离合成浏览器证据，不等于原生、Windows 或发行验收；现用安装与真实偏好未动。自动缩放计算、布局和视觉资产继续保留归因及 LICENSE/NOTICE。下一步处理边缘吸附或皮肤选择的剩余事件。
