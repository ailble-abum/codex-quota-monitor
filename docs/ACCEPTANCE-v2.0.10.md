# v2.0.10 发行验收记录

日期：2026-09-26。使用合成页面和独立临时目录；未修改现用监视器、真实认证或会话记录。

- Chromium/WebKit 面板挂载回归验证同一手势的指针归属、第二个触点被忽略、取消拖动恢复原布局且不触发落地；既有鼠标抚摸、拖动、点击与六款换帧继续通过。这是浏览器中合成的触控 `PointerEvent`，不是触控硬件验收。
- Python 全套 210 项测试、Node 伴宠运行时与行为探针、Swift 类型检查通过。隔离预览包含 68 个文件，清单审计通过；Chromium/WebKit 候选 consumer 挂载回归通过。
- 正式包内回归与摘要：待完成。
- 真实 macOS Codex UI：本轮无法通过计算机操作接口访问该应用；没有触碰现用安装，故不计通过。Windows 真机、真实触控设备、签名和公证亦未验收。
- 发行页文件与重新下载校验：待完成。
