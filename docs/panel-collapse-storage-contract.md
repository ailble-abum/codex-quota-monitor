# 折叠偏好存储容错

2026-09-20，接续 ce5c186。V2 panel_mount 的折叠读取与写入仍未捕获 Storage 异常，本轮直接补齐两处处理，无新模块或依赖。

读取失败默认展开；写入失败保留已改变的 DOM 折叠状态，继续按钮文字、标题及 keepTogglePosition 布局更新。临时状态不声称已持久化，卸载/重启仍读取旧记录。其他存储访问的边界不因此消失。

## 验证

- verify_mount 在旧候选中只拒绝折叠键读取时初始化失败；新候选 Chromium/WebKit 完整挂载与设置回归通过。
- verify_docking 在旧候选中两次切换各抛出 full 异常；新候选双浏览器左右停靠在写入拒绝下完成折叠/展开往返，保持固定停靠可见且无页面异常，后续尺寸/重置回归通过。
- Python 3.9.6 / 3.14.3 各 120 项通过。

候选 /tmp/quota-collapse-candidate，截图 /tmp/quota-collapse-evidence。隔离合成浏览器证据不等于原生、Windows 或发行验收；现用安装与真实偏好未动，保留布局及视觉代码继续归因。下一步整理剩余几何、标题与伴宠渲染的替换边界。
