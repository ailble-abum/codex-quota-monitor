# 插件伴宠高清与交互实施计划

用户已确认 2026-09-20 聊天中的方案并授权开始；目标是额度监控插件的六款伴宠，不是 Codex 自定义宠物。

1. 复用现有悬浮层、quota_reader、当前任务摘要。新增可独立测试的本地提醒状态机，覆盖账户隔离、任务切换、未知数据、阈值去重、真实恢复；额度 20/10/0%，CTX 70/85/95%。
2. 集成状态动作与文字反馈、摸头/点击/拖拽区分、关闭动画设置和系统减少动画。保留点击固定与边缘拖动。仅匹配当前可见任务；压缩只使用明确日志事件，不推断正在压缩。
3. 根据最大显示尺寸与 Retina 倍率改善已有高清素材导出。保持角色身份和透明边缘，不将纯放大声称为重绘动画。
4. 合成数据验证额度下降/恢复、CTX 切换与提示消失、触碰/拖动、深浅主题。Python 全量测试、Swift typecheck；更新版本与安装后 doctor，分别报告模拟和真实验收。

复用依据：本仓库已有六款角色与相关数据接口；2026-09-20 核对 https://developers.openai.com/zh-Hans/docs/app-server，官方有 account/rateLimits/read、thread/tokenUsage/updated、contextCompaction。第一版沿用只读本地链路，不新增连接以假定可以订阅桌面已有任务。无新依赖。

## 后续授权：其余五款表情

用户在黑猫交付后明确要求继续补齐软糖女孩、柯基、薄荷萌男、霜夜先生和红茶御姐。沿用同一六状态接口与交互规则，分别以各自原有形象生成透明图集，扩展既有构建脚本；逐款检查身份、透明度、动作语义和固定画布。浏览器验收扩展到全部六款，覆盖六表情、摸头、额度/CTX反馈、任务隔离、左右贴边与深浅主题。所有素材通过后再更新缓存版本、安装、提交并推送。

用户对总览复查指出 candy / mint 的 notice 帧肩袖与双手连接不自然。增加专项验收：这两格局部重绘为单只自然抬手，另一手隐藏在右边缘后；其他五格保持原图，放大检查肩、肘、袖口、手腕连续性后重新构建和验证。

完成记录：六款36表情已生成并集成；147项Python测试、Swift typecheck、六款双引擎交互及两侧布局检查通过；两张手臂修复帧通过追加复测。runtime35 本机安装与 doctor 已通过。详见 docs/companion-feedback.md。
