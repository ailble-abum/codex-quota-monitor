# 2026-09-26 第三轮：悬停读取与紧凑栏

本轮对应用户两张实景截图：侧栏一直显示“正在读取”；紧凑栏把估算可用时长放在主值位置，CTX 下方显示压后 token 数，右侧留白偏大，“+”字形偏下。

后续悬停压缩摘要、人物小柱和自动收回交互修复见 [第四轮记录](FIXES-2026-09-26-ROUND4.md)。

## 修复清单

| 问题 | 原因与本轮处理 |
| --- | --- |
| 悬停摘要迟迟不出现 | 原来全部摘要的发布依赖当前对话先读完。现在当前对话与悬停对话独立判断是否读完，已完成的悬停摘要可先显示；仍核对文件名、日志内身份与唯一性。 |
| 超大日志只有一句加载提示 | 截图中的“导演”日志只读检查约 466 MiB。首次扫描显示字节进度对应的百分比；未写完的尾行不伪装成 100% 完成。 |
| 目录变化时无明确状态 | 重扫描等待期间继续推进已知日志游标，显示列表更新状态；在重扫确认唯一性前不发布旧摘要，避免新增同 ID 日志被误关联。未选中本地对话时明确提示打开本地对话。 |
| 额度主值显示 8.8h | 主值恢复为剩余额度百分比；估算时长显示为小字“约 8.8h”，无估算时不补造数值。 |
| CTX 下方 52.3K 难理解 | 改为“↻3 · 压后 20.2%”这种形式，表示最近一次压缩后的首次真实请求占上下文窗口的比例；未观察到后续请求时显示等待，不写 0%。 |
| 右侧空白和展开按钮偏下 | 收紧间距及默认宽度，保留用户缩放；用 CSS 横竖线绘制几何居中的加/减图标，同时保留按钮名称和键盘操作。 |

不新增依赖，复用现有增量日志读取器、官方额度结果与压缩统计字段。没有生成或替换人物图片，没有修改 Codex 本体、认证或会话记录。

## 验证

- 使用临时合成日志覆盖 UUID 文件名、本地别名、当前日志未完成而悬停已完成、字节进度、尾行等待、目录重扫和重复身份。
- Chromium/WebKit 验证单额度窗口、两个额度窗口、无估算、未知数据、缩放、文字不裁切，以及图标与按钮和整栏中心的关系。
- 紧凑栏验证图位于 `.local/compact-hud-round3-evidence-e/`，图中 62% 等是合成测试数值，并非本机实时额度。
- 完整链路额外覆盖当前日志写入半行时悬停继续显示、主 CTX 清空及日志补全后恢复，防止把旧 CTX 当成当前数据。

- 最终 Python 全量 **241 项通过**，无跳过；Chromium 明暗两主题完整运行链路通过，覆盖悬停已就绪时活动日志未写完、旧 CTX 清空和写入完成后恢复。
- 额外验证完整日志与同 ID 未完成日志同时存在时不发布数字；另一候选尚未读到身份时也不会跳过唯一性核查。

## 本机部署

- 2026-09-26 安装源码 `5bec186`，包含全部本轮修复；上游基础版本仍为 2.0.11，本轮没有推送远端。
- 本轮覆盖 5 个运行模块和 2 个构建产物；安装前后 68 个发行文件按上游清单加本机差异清单校验通过。
- renderer SHA-256：`db4df3cad4ba914ed61da855f8ebbce660f08253b61ea3806003b405fd5ee7f2`。
- 原启动器、两个启动服务配置、菜单程序及上游清单共 10 个文件哈希不变；保留手动启动方式、19222 和无 `host_app` 配置，不备份旧运行代码。
- 只重启监视器，没有退出或重开 Codex。主服务和顶栏服务均运行、配置有效；页面诊断由最初的 `unselected` 转为 `data_loading`，即已识别当前本地对话并开始读取日志。实际 Codex 窗口的本轮悬停效果尚未验收，不能以安装和浏览器测试替代。
- 部署摘要保存在本机 `.local/round3-deployment.json`。纯文档安装记录不增加行为测试。

## 边界

超大日志的首次完整读取仍需要时间；百分比表示实际文件读取进度，不是完成时间承诺。真实 Codex 窗口的鼠标交互效果与合成浏览器验证分开记录。本轮不做 Windows 真机或新发行版验收。
