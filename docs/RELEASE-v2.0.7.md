# Codex Quota Monitor v2.0.7 · macOS

修复历史区与会话说明中的长模型名、项目名被横向裁切的问题。连续字符会在可用宽度内换行，文本与数据本身不变。

已安装 v2.0.3 或更新版本的用户可在面板点击“检测更新”，选择“安装并重启”。更早版本需手动下载 ZIP 与 `SHA256SUMS`，校验后按包内 `RELEASE.txt` 操作。旧目录和 Python 虚拟环境应保留以便回退。发行包未签名或公证。

发行包继续包含 Kevin Ke 与 Ailble 的 MIT `LICENSE` 和 `NOTICE`。测试范围见[验收记录](ACCEPTANCE-v2.0.7.md)。
