# V2 最新交接（2026-09-20）

## 当前目标——以此覆盖早期方向

用户已明确：**暂不考虑官方插件接入，独立替换继承的上游代码，保留现有功能和 Codex 内嵌额度条。** “消除上层代码”已澄清为“消除上游代码”。不接受独立窗口；不要重新以官方接口缺口阻塞离线源码替换，也不要反复调查已记录的官方能力。

沿用 [剩余 renderer 替换路线](independent-renderer-roadmap.md)，按行为规格独立实现，不复制、逐行改写或仅重命名旧代码。仍包含旧实质性代码/资源的产物保留 LICENSE/NOTICE；函数数量、新 Git 历史或相似度不能证明独立著作权，不宣称法律审计通过。

普通打开 Codex 后自动跟随、退出后不擅自重开，仍是产品目标，但与本轮离线源码替换分别验收。真实宿主操作限制没有解除。

## 仓库与提交

- 仓库：`/Users/ailble/Documents/ChatGPT/codex剩余额度/codex-quota-monitor-v2`
- 分支：`codex/page-update-loop`
- 最新产品提交：`0741bea`，本次交接前工作区干净；交接另作纯文档提交。
- `0741bea`：独立替换四个时间展示辅助函数。
- `5b21dc0`：剩余 renderer 来源盘点与替换路线。
- `89c881a` / `335a7dc`：畸形候选清单受控失败修复及独立复核记录。
- `bf21355`：官方插件能力核查，现作为参考，不是当前主线前置条件。
- `ddb3e91`：可选 `--wait-for-host` 安静等待与恢复，已完成，不重复实现。

## 最新完成：四个时间辅助函数

`quota_monitor/panel_time.js` 独立提供 shortDuration、durationPhrase、windowLabel、nearestResetText。有效数值展示保持兼容；非有限值/非数值时长显示 `-`；无效窗口周期回退名称；重置时间只接受 0..8.64e12 Unix 秒。

`tools/build_panel_candidate.py` 在移除既有旧预算函数后，裁剪 shortDuration 到 pressure 前的区域，插入新模块。已核对输出中四个函数各一份，LICENSE/NOTICE 与冻结来源字节一致。详见 [时间展示契约](panel-time-contract.md)。

本次交接没有重跑以下测试，它们是上一实现轮次的已执行证据：

- `node tools/verify_time_format.cjs`：双语、舍入边界、输入校验、最早有效时间与日期上下界通过。
- `verify_account_overview.cjs`：使用真实新时间模块，Chromium/WebKit 预算、失效清空、双语通过。
- `verify_mount.cjs`：完整候选挂载、控件、5h/7d 标签与最近重置 DOM 断言通过。
- `verify_panel.cjs --bridge`：Chromium/WebKit × 明暗主题，任务切换、缺失、过期和单实例通过。
- WebKit 完整面板与配额卡片的明暗截图已目视核对。
- Python 3.9.6 / 3.14.3 全套各 136 项通过；JS 检查另计。
- Luna Max 独立复核未发现逻辑错误或误删；补充其建议的独立超日期上界断言后通过。

这些仅为单元/合成浏览器证据，不是现用安装、真实账户、真实原生窗口、Windows 或发行验收。

## 下一步具体工作

1. 先读 AGENTS.md，核对分支、git status 和最近提交；保留其他人的改动。不要重复等待循环、安装器修复或四个时间函数。
2. 从阶段 1 剩余指标辅助入手：建议先提取 quotaTone、accountTone、contextTone、contextMeterValue、remainingContext、toneLabel、gaugeReading/gaugeColor 的行为契约、调用点和相邻边界。先确认是否含本项目新增实现，不能把“外部保留”一概等同于“上游贡献”。
3. 复用已有 V2 账户时效/窗口校验、数值格式化和界面语言模块；按一组共享行为独立替换，补先失败后通过的边界测试，接入真实调用链的隔离浏览器验证。
4. 核对构建产物不再保留该组旧定义、无重复定义、归因未丢失；保持深浅主题和交互。简单重复工作交给 Luna Max，避免派多个 Agent 重复扫描全部旧源码。
5. 配套相关测试、复核与 commit；更新路线中的已完成和开放项。后续才处理布局/拖动/停靠、模板/CSS/伴宠装配、最终独立构建。

阶段 1 尚未整体完成。同口径具名函数定位由 59 降到 55（含通用局部函数），只是来源定位指标；最终构建仍需旧仓库冻结输入。完整替换目标是发行构建不再读取旧仓库脚本或提取旧 HTML/CSS/函数，资源来源可追溯。

## 冻结来源与重建

旧仓库：相邻 `../codex-quota-monitor`。固定提交：`2705f32a6f080ee1bdbecdc5b43f5fe0f9045eed`。

所需文件：仓库根 LICENSE、NOTICE，以及 `plugins/codex-quota-monitor/scripts/` 下 context_token_injector.py、companion_feedback.py、companion_art.py、companion_expressions.py。注意不是仓库根 scripts/。通过 `git show COMMIT:PATH` 读取到新的临时目录，勿 checkout 或改动旧仓库。构建器会检查六个文件 SHA256；不要取消校验或对未知版本容错拼接。

```sh
python3 tools/build_panel_candidate.py /absolute/frozen-input /absolute/new-candidate
node tools/verify_time_format.cjs
node tools/verify_account_overview.cjs
node tools/verify_mount.cjs /absolute/new-candidate/consumer.js /absolute/card-evidence
QUOTA_PANEL_CANDIDATE=/absolute/new-candidate/consumer.js node tools/verify_panel.cjs /absolute/frozen-input /absolute/panel-evidence --bridge
python -m unittest discover -s tests
```

浏览器脚本使用已有 Playwright，可设置 NODE_PATH；本轮使用 `/Users/ailble/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules`。Python 依赖见 requirements-cdp.txt（websockets 15.0.1），应使用独立环境。路径可能变化，缺失时重新定位环境，不修改现用安装。

临时证据（可能被清理，不作为唯一可重建来源）：候选 `/tmp/quota-time-candidate-v2`，截图 `/tmp/quota-time-panel-evidence`、`/tmp/quota-time-cards-evidence`；Python 环境 `/tmp/quota-v2-validation-20260920`（3.14）、`/tmp/quota-v2-validation-py39-20260920`（3.9）。

## 现用安装与未完成项

现用安装历史记录：`~/Library/Application Support/CodexQuotaMonitorV2/preview-20260920-switch`；LaunchAgent `~/Library/LaunchAgents/local.codex-quota-monitor-v2.plist`，标签 local.codex-quota-monitor-v2。此前只读核对 RunAtLoad/KeepAlive 为 true，运行参数没有 --wait-for-host，安装副本没有等待模式。仓库提交不会自动更新安装。本次源码替换未部署，当前服务健康未重新检查。

真实窗口刷新曾通过；整应用退出重开未通过。等待模式只能等待已有调试端点，不能自动开启端口。不得把合成浏览器关闭重建恢复当作普通图标启动通过。Windows 真机、正式发行及完整来源审查仍未完成；没有上传。

## 不可绕过的操作限制

此前 Computer Use 明确拒绝：`Computer Use is not allowed to use the app 'com.openai.codex' for safety reasons.`

不得借新对话、CDP、AppleScript、CLI、后台任务、旧脚本或另一 Agent 绕过同一限制。可以只读源码、提取行为规格、在临时合成数据/浏览器中测试；不得修改真实认证、会话记录或现用监视器安装。

不要重复 `launchctl submit` 重启探针：此前被当作 KeepAlive 任务重复运行，覆盖失败记录。相关探针已清除。保留旧版文件和现有归因，不擅自清理真实安装或回退材料。

## 可复制接续提示词

接手 `/Users/ailble/Documents/ChatGPT/codex剩余额度/codex-quota-monitor-v2`。先读 AGENTS.md、docs/HANDOFF-2026-09-20.md、docs/independent-renderer-roadmap.md。当前主线是独立替换继承的上游代码，暂不考虑官方插件接入；保留现有功能及 Codex 内嵌额度条。最新产品提交 0741bea 已替换四个时间辅助函数，双 Python 各136测试、双浏览器与明暗视觉及独立复核通过，但未部署。继续阶段1剩余指标辅助，先提取规格和调用边界，再独立实现、合成验证并提交；不要重复已完成工作，不提前删除归因，不绕过真实Codex操作安全限制。
