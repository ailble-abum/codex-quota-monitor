# V2 接手续作记录（2026-09-20）

## 后续核查入口

最新用户方向：暂不考虑官方插件接入，继续独立替换继承的上游代码，保留现有功能及内嵌界面。见 [剩余 renderer 替换路线](independent-renderer-roadmap.md)。官方接口缺口不再作为离线源码替换的前置条件；真实宿主操作限制、归因和分层验收要求保持有效。

`89c881a` 独立代码复核已完成：清单结构校验和 8 类回归未发现阻塞问题；复核中的 bundle 聚焦测试 2 项通过。历史 130 项测试与最近双版本各 136 项测试已明确区分。该结论仅覆盖离线安装器修复，不表示宿主接入或发行验收通过。

离线实现接续：已修复预览安装器对畸形清单顶层调用 `.get()` 导致 traceback 的问题，增加结构校验及 8 类合成输入回归。Python 3.9/3.14 各 136 项通过；详见 [账户读取与预览安装](account-and-preview.md)。这一步独立于宿主集成，不改变下述安全限制、现用安装或内嵌目标，普通启动跟随仍未完成。

此前已追加 [官方插件内嵌与启动能力核查](official-plugin-feasibility.md)：确认文档中的插件安装、命令钩子、本地 MCP 进程及对话 UI 能力；未找到第三方常驻侧栏挂载接口，不能将官方 Security 工作台能力泛化。该次核查仅文档更新，现用安装未改变，普通启动自动跟随仍未完成。下文保留原交接时点的记录。

## 用户已经明确的产品要求

- 额度条必须显示在 Codex 软件内部，保留当前截图中的内嵌体验。用户明确拒绝独立窗口方案，不要再次改推独立窗口。
- 希望恢复旧版体验：正常打开 Codex，面板自动跟随出现；以后不需要每次运行终端命令。退出应用后不能擅自重开。
- 后续目标是发布到 Codex 官方插件市场。市场是否支持这种内嵌方式尚未核查，不得承诺可以上架。
- 用户允许参考旧版机制，使用自己的代码和设计；已经解释：换设计不能消除许可证、调试注入、兼容性或上架风险。现有复用部分仍要保留来源归因，不能宣称已经完全自主或法律审计通过。
- 曾授权本机安装与切换，先验证可用再上传；目前未上传。本次仅交接，不要求创建新任务。

## 仓库与提交

仓库：`/Users/ailble/Documents/ChatGPT/codex剩余额度/codex-quota-monitor-v2`
分支：`codex/page-update-loop`

- `ddb3e91`：增加可选 `--wait-for-host`，宿主端点不可用或页面未出现时每 15 秒重试，保持同一进程；恢复后接续。默认关闭，单次模式、其他错误上限与信号清理保留。
- `a8081d1`：记录原生刷新通过、整应用重启失败及恢复边界。
- `d60633f`：本机任务定位、预算显示修正及本机 V2 切换。

交接前代码工作区干净。本交接文件另作纯文档提交。

## 已有证据与未完成项

`ddb3e91` 已在 Python 3.9、3.14 各通过 135 项测试。`tools/verify_live.cjs` 使用临时合成 Chromium 目标，两轮关闭与重新打开后，同一 CLI PID 恢复更新、每页一个消费者、SIGTERM 正常退出。只读复核没有发现具体问题。这些是此前执行结果，本次文档交接未重跑。

真实窗口刷新曾通过；整应用退出与重开验收未通过。不能把临时浏览器验收记为真实 Codex 启动通过。

等待模式还未部署到现用安装。它不能在端点未开启时自行开启端口，因此正常图标启动后自动跟随仍未完成。

旧版启动器在发现用户打开应用但没有调试端口时，会退出并带调试参数重开应用；仅启动监视器不能复现该效果。V2 尚未实现这一环节。

## 现用安装（历史记录，接手时只读核对）

- 安装目录：`/Users/ailble/Library/Application Support/CodexQuotaMonitorV2/preview-20260920-switch`
- LaunchAgent：`/Users/ailble/Library/LaunchAgents/local.codex-quota-monitor-v2.plist`
- 标签：`local.codex-quota-monitor-v2`；此前 RunAtLoad/KeepAlive 开启，命令中没有 `--wait-for-host`。
- 安装是源码副本，仓库提交不会自动更新安装。
- 用户曾手动恢复带调试端口的启动；监视器自身日志随后显示 updated。此后状态需要重新核对，不等于已验收普通启动。
- 旧版服务此前已停用，旧文件保留作回退。

## 重要操作限制与历史故障

此前 Computer Use 明确返回：`Computer Use is not allowed to use the app 'com.openai.codex' for safety reasons.`

不得把新对话当作清除该限制的方式，也不能改用 CDP、AppleScript、CLI、后台任务、旧版脚本或另一 Agent 绕过同一操作限制。用户授权不消除工具安全限制。可以开展源码、官方接口研究和不接触真实应用的合成测试；不要承诺可以部署后台重开方案。

曾使用 `launchctl submit` 做一次性重启探针，但该环境将其作为 KeepAlive 任务重复运行，造成失败记录被覆盖。探针和验证用任务已清除；不要重复该做法或宣称重启已通过。

## 建议接手顺序

1. 阅读 AGENTS.md，核对 git 状态与实际安装记录，保留现有改动及当前用户运行状态。
2. 先用 openai-docs 技能核查官方插件机制、可用 UI 扩展点和分发要求，优先本地官方资料，必要时查官方文档；记录链接、核查日期，以及已证实与未知的边界。
3. 将“插件可安装”“插件能启动进程”“官方支持向现有侧栏嵌入界面”分别核实。现有调试注入不构成官方侧栏扩展支持的证据。
4. 在保留内嵌目标的前提下，提出有依据的接入路径；若官方资料未提供相应能力，明确说明未找到支持，不擅自改成独立窗口。
5. 只有存在允许实施的路径才继续开发。参考旧版行为时不复制、逐行改写或仅重命名旧实现；保持许可证与来源记录。禁止借参考方案绕过上述工具拒绝。
6. 每次改动配套测试并提交。区分单元、合成浏览器、真实 macOS、Windows和上架证据。未完成不得记为通过；未完成本机验收不要上传。

相关资料：`docs/host-follow.md`、`docs/native-recovery.md`、`docs/local-switch.md`、`docs/source-audit.md`、`docs/account-and-preview.md`。

## 给下个对话的简短提示词

接手 `/Users/ailble/Documents/ChatGPT/codex剩余额度/codex-quota-monitor-v2`。先读 `AGENTS.md` 和 `docs/HANDOFF-2026-09-20.md`。必须保留 Codex 内部的额度条，用户拒绝独立窗口，未来目标是官方插件市场。先核实官方 UI 扩展和启动机制，再决定实施路径；不要重复已完成的等待循环，也不要绕过已有的 Codex 操作安全拒绝。核对实际状态后推进有证据、可验收的一步。
