# v2.0.5 发行验收记录

日期：2026-09-25。使用合成账户活动与独立临时目录，不修改现用监视器、认证或会话记录。

- Python 3.9 全套 208 项测试通过；其中菜单命令模式验证最近日用量、累计活动、账户隔离及过期隐藏，本地采样测试验证只保存 live 账户的有效数值。
- Swift 类型检查通过；macOS 菜单程序已编译为 arm64 和 x86_64 通用 Mach-O。隔离预览通过文件清单和摘要审计。
- 菜单 Token 活动复用现有官方 `account/usage/read` 的 V2 投影。这里验证了合成响应到本地采样及菜单命令输出，没有用真实账户活动验收原生菜单画面。

待验证：真实 macOS 菜单画面、Windows 真机、签名和公证。发布包摘要与公开下载核对另在发布后记录。
