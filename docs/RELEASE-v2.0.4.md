# Codex Quota Monitor v2.0.4 · macOS

修复面板拖动卡顿：拖动期间关闭位置动画、直接更新位置，松手后再保存；缩放和猫咪拖动也只在结束时保存。菜单栏补回额度窗口与上下文数据。

已在 Python 3.9 环境通过 207 项测试，并通过布局、猫咪行为和样式脚本校验。保留 Kevin Ke 与 Ailble 的 MIT `LICENSE` 和 `NOTICE`。

已安装 v2.0.3 的用户可在面板点击“检测更新”，选择“安装并重启”。首次安装请下载本版 ZIP 与 `SHA256SUMS`，校验后按包内 `RELEASE.txt` 操作。发行包尚未签名或公证。
