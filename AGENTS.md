# 开发协作规则（Agent 必读）
本文件为 AI Agent 在本仓库协作时的强制约定，每次改动前先阅读。
## 规则一：每次改动必须提交

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- commit message 遵循仓库现有风格（`type(scope): 中文描述`）。
- 阶段性改动（如一次任务拆成多步）可拆多个 commit，但交付前必须全部提交，不留未提交改动。

## 规则二：每次改动必须配套测试

- 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。
- 若某类改动不适用测试（如纯文档），需在提交说明中注明原因。

## 规则三：需求评估与优先复用（长期规则）
- 评估新需求、功能升级或替代方案时，先检查项目已有实现，并搜索 GitHub、官方文档中成熟或可用的开源项目、库、工具和接口，再决定是否自研。优先在可复用的基础上补齐差异，避免重复开发。
- 调研与需求澄清可以同时进行；用户已明确的需求不反复确认。简单修复无需为了流程扩大调研，但涉及新能力或技术选型时应先完成有针对性的检索。
- 按实际功能匹配度、维护状态、相关问题反馈、许可证、技术栈与打包成本、登录态及数据处理方式评估候选；不能仅凭 Star 数、宣传或“支持某网站”判定可用。
- 优先考虑直接依赖或调用现有能力，其次提取项目内公共模块、在许可允许的范围内复用或改造开源实现；仅对未覆盖的部分自研。保留上游来源、采用版本及必要声明。
- 将选型依据记录到相关需求文档：候选链接、核查日期、可复用部分、接入方式、验证结果、限制，以及采用或不采用的理由。明确区分“文档宣称支持”“源码已核对”和“实际场景已验证”。
- 外部方案先以最小案例验证关键路径，再扩大集成；复用的验收标准与自研一致，不能把成功启动、解析出 URL 或下载请求完成直接当作用户目标已实现。

## 本项目补充约定
- 中文沟通；简单重复工作交给 Luna Max。
- V2 目标是独立替换 KevinKE93/Codex-Monitor 派生实现。不得复制、逐行改写或仅重命名旧实现；依据来源已记录的接口与行为规格实现。
- 新目录、新 Git 历史、相似度结果都不构成独立著作权证明。不得提前删除旧版归因或宣称法律审计通过。
- 验收使用合成会话和独立临时目录；不得修改真实认证、会话记录或现用监视器安装。
- 单元测试、真实 macOS UI、Windows 真机和发行验收分别记录。
